# HUD Package Definition - libtirpc 1.3.7 (NEWER VERSION)
# Transport Independent RPC library (replaces older glibc RPC)
# Required by: glusterfs, nfs-utils
# Reference: https://sourceforge.net/projects/libtirpc/
#
# Using version 1.3.7 which has better GCC compatibility

Package: libtirpc
Version: 1.3.7
Architecture: x86_64
Section: libraries
Description: Transport Independent RPC library
Source: https://downloads.sourceforge.net/libtirpc/libtirpc-1.3.7.tar.bz2
Source-SHA256:    b47d3ac19d3549e54a05d0019a6c400674da716123858cfdb6d3bdd70a66c702

Build-Depends:    
Depends:          auto
Provides:         auto

[configure]
export PATH="/opt/hud/bin:/opt/hud/sbin:${PATH}"
export PKG_CONFIG_PATH="/opt/hud/lib/pkgconfig:/opt/hud/lib64/pkgconfig:${PKG_CONFIG_PATH}"
export LD_LIBRARY_PATH="/opt/hud/lib:/opt/hud/lib64:${LD_LIBRARY_PATH}"
export CFLAGS="-I/opt/hud/include -O2"
export LDFLAGS="-L/opt/hud/lib -L/opt/hud/lib64"

./configure --prefix=/opt/hud \
            --sysconfdir=/etc \
            --libdir=/opt/hud/lib64 \
            --disable-static \
            --disable-gssapi

[build]
export PATH="/opt/hud/bin:/opt/hud/sbin:${PATH}"
export LD_LIBRARY_PATH="/opt/hud/lib:/opt/hud/lib64:${LD_LIBRARY_PATH}"

make -j$(nproc)

[install]
make DESTDIR=$DESTDIR install

# Compatibility symlinks and the loader drop-in. Same split as gperftools: v1
# staged a partial set here and created a different, fully versioned set in
# [postinst], where nothing tracked it.
install -v -dm755 $DESTDIR/usr/lib64 $DESTDIR/usr/include $DESTDIR/etc/ld.so.conf.d
ln -sfn /opt/hud/lib64/libtirpc.so.3 $DESTDIR/usr/lib64/libtirpc.so.3
ln -sfn libtirpc.so.3                $DESTDIR/usr/lib64/libtirpc.so
ln -sfn /opt/hud/include/tirpc       $DESTDIR/usr/include/tirpc
echo "/opt/hud/lib64" > $DESTDIR/etc/ld.so.conf.d/libtirpc.conf

[postinst]
# Everything this used to create is shipped by [install] now.
ldconfig

echo ""
echo "============================================"
echo "libtirpc 1.3.7 installed successfully"
echo "============================================"
echo ""
echo "Library: /opt/hud/lib64/libtirpc.so"
echo "Headers: /opt/hud/include/tirpc/"
echo ""
echo "This replaces deprecated glibc RPC functions"
echo "with a modern, maintained implementation."
echo ""
